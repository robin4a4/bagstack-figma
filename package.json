{
  "name": "bagstack",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && esbuild src/main.ts --bundle --outfile=dist/main.js --watch",
    "build": "tsc && esbuild src/main.ts --bundle --outfile=dist/main.js"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "*"
  },
  "dependencies": {
    "esbuild": "^0.14.51"
  }
}
